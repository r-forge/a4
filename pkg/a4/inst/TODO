TODO list for the a4 package

o spectralMap add legend 

o invisibly return histogram breaks for histPvalue function 
  (as in plain hist function)

o document the following objects

Undocumented code objects:
  
  histpvalueplotter 
  
  lassoReg plot1gene plotCombination2genes
  propdegenescalculation

o plot1gene(tTestResult, 
        geneId = NULL,            (if is.null ---> most significant one)
        probesetId = NULL,
        rankNumber = 1,
        rankCriterion = c("significance", "logratio")
    
o plot1gene methods for 
      - pam
      - randomForest
      - limma
        
o zelfde aanpassing voor plot2Combinations (als voor plot1gene)
                  
o   [ extraction voor limmaResult

o   topTable limma; verwijderen van Description (nu inline)
o   annotationTable voor alle topTables

o   write method for spectralMap with object data.frame and groups factor

o   global option probe2gene (topTable, etc.)
    in order to allow use of a4 for analysis of non-expression data

o   INI-calls Steven

o   a4palette vergelijken met de code die Steven gebruikt
    om de kleuren in het ExpressionSet object te stoppen

o   fdr2d
   

   